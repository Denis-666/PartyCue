# OCR Smoke Fixtures

This folder keeps the one-off OCR smoke page and image variants out of the extension root.

- `index.html`: opens a local browser smoke page against the bundled Tesseract assets.
- `fixtures/`: captured crops and preprocessing variants used while tuning OCR.
