{
  "name": "partycue-now-playing",
  "version": "1.0.0",
  "description": "A cheerful Chrome extension that OCRs the now-playing label on a target YouTube radio stream and opens NetEase Cloud Music.",
  "scripts": {
    "check": "node --check src/background.js && node --check src/popup.js && node --check src/content.js && node --check src/offscreen.js"
  },
  "keywords": [
    "chrome-extension",
    "netease-cloud-music",
    "now-playing",
    "ocr",
    "party",
    "youtube"
  ],
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "@tesseract.js-data/eng": "^1.0.0",
    "tesseract.js": "^7.0.0",
    "tesseract.js-core": "7.0.0"
  }
}
